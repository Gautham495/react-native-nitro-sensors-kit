///
/// HybridPedometerSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `PedometerData` to properly resolve imports.
namespace margelo::nitro::nitrosensorskit { struct PedometerData; }
// Forward declaration of `PermissionStatus` to properly resolve imports.
namespace margelo::nitro::nitrosensorskit { enum class PermissionStatus; }

#include "PedometerData.hpp"
#include <functional>
#include <optional>
#include "PermissionStatus.hpp"
#include <NitroModules/Promise.hpp>

namespace margelo::nitro::nitrosensorskit {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Pedometer`
   * Inherit this class to create instances of `HybridPedometerSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridPedometer: public HybridPedometerSpec {
   * public:
   *   HybridPedometer(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridPedometerSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridPedometerSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridPedometerSpec() override = default;

    public:
      // Properties
      virtual bool getIsAvailable() = 0;
      virtual bool getIsActive() = 0;
      virtual std::optional<std::function<void(const PedometerData& /* data */)>> getOnUpdate() = 0;
      virtual void setOnUpdate(const std::optional<std::function<void(const PedometerData& /* data */)>>& onUpdate) = 0;

    public:
      // Methods
      virtual std::shared_ptr<Promise<PermissionStatus>> checkPermission() = 0;
      virtual std::shared_ptr<Promise<PermissionStatus>> requestPermission() = 0;
      virtual void start() = 0;
      virtual void stop() = 0;
      virtual std::shared_ptr<Promise<PedometerData>> queryHistoricalData(double startTime, double endTime) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Pedometer";
  };

} // namespace margelo::nitro::nitrosensorskit
